var bucketList = ['Go to Korea', 'SkyDiving', 'MotoCross'];
var listItem = bucketList[1];

bucketList.push('Go to Boracay', 'Go to Hollywood');
bucketList.pop();

console.log(listItem);
console.log(bucketList.length);
console.log(bucketList);
